# MacPython/MacRoman

::: {#content dir="ltr" lang="en"}
[MacRoman](./MacRoman.html){.nonexistent} is a single byte text encoding particular to MacOS that shares quite a bit with 7-bit ASCII. Python calls it `mac_roman` (the encodings module) or `macroman` (the more common alias).
:::
